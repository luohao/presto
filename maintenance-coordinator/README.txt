Presto Maintenance Coordinator

This is a service for Aurora's Coordinator-based SLA policy. It expose an endpoint that will be called to check if removal of a task will affect the SLA requirements for the job.
